{
  "name": "tidal-cli",
  "version": "0.1.0",
  "description": "CLI to control the TIDAL desktop app via Chrome DevTools Protocol",
  "main": "dist/cli.js",
  "bin": {
    "tidal-cli": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "npm run build && node --test tests/*.test.js"
  },
  "keywords": ["tidal", "music", "cli", "cdp", "devtools"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^25.2.1",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "ws": "^8.18.1"
  }
}
